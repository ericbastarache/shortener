import React, { Component } from 'react';

class URL extends Component {
  render () {
    return (
      <div>
        <li style={{borderBottom: '1px dashed #ccc'}}>
          <a href="#">Some URL</a>
        </li>
        <li style={{borderBottom: '1px dashed #ccc'}}>
          <a href="#">Some URL</a>
        </li>
        <li style={{borderBottom: '1px dashed #ccc'}}>
          <a href="#">Some URL</a>
        </li>
      </div>
    );
  }
}

export default URL;
